<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr"
    crossorigin="anonymous"
  />
  <style>
    
  .dropdown-menu {
    background-color: #fffafa;
    border: 1px solid #f8d7da;
    border-radius: 6px;
    box-shadow: 0 6px 12px rgba(231, 76, 60, 0.1);
    padding: 8px 0;
    min-width: 180px;
  }

  .dropdown-item,
  .nav-item .nav-link {
    padding: 10px 16px;
    color: #c0392b;
    font-weight: 500;
    font-size: 14px;
    text-decoration: none;
    display: block;
    transition: background-color 0.2s ease, color 0.2s ease;
  }

  .dropdown-item:hover,
  .nav-item .nav-link:hover {
    background-color: #fceeee;
    color: #a93226;
  }

  /* Optional: style for disabled or inactive links */
  .nav-item .nav-link[aria-disabled="true"] {
    color: #999;
    pointer-events: none;
    cursor: not-allowed;
  }

  /* Optional: Add icon spacing if needed later */
  .dropdown-item i,
  .nav-link i {
    margin-right: 8px;
  }

  /* On mobile, ensure dropdowns are full-width */
  @media (max-width: 768px) {
    .dropdown-menu {
      min-width: 100%;
    }
  }

  .navbar {
    background-color: #fff5f5 !important;
    box-shadow: 0 2px 8px rgba(231, 76, 60, 0.1);
    padding: 10px 20px;
  }

  .navbar-brand {
    color: #e74c3c !important;
    font-weight: 700;
    font-size: 24px;
  }

  .navbar-nav .nav-link {
    color: #c0392b !important;
    font-weight: 500;
    margin-right: 15px;
    transition: color 0.3s ease;
  }

  .navbar-nav .nav-link:hover,
  .navbar-nav .nav-link.active {
    color: #a93226 !important;
  }

  .navbar-nav .dropdown-menu {
    background-color: #fffafa;
    border: 1px solid #f5cccc;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
  }

  .navbar-nav .dropdown-item {
    color: #c0392b;
    transition: background 0.2s ease;
  }

  .navbar-nav .dropdown-item:hover {
    background-color: #fceeee;
    color: #a93226;
  }

  .navbar-toggler {
    border: none;
  }

  .navbar-toggler-icon {
    background-image: url("data:image/svg+xml,%3csvg viewBox='0 0 30 30' xmlns='http://www.w3.org/2000/svg'%3e%3cpath stroke='rgba(192, 57, 43, 0.8)' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
  }

  img[alt="avatar"] {
    width: 40px;
    height: 40px;
    object-fit: cover;
    border-radius: 50%;
    margin-left: 15px;
    border: 2px solid #e74c3c;
  }

  @media (max-width: 768px) {
    .navbar-nav {
      margin-top: 10px;
    }

    img[alt="avatar"] {
      margin-top: 10px;
    }
  }

  h3 {
    color: #e74c3c;
    margin-bottom: 30px;
    font-size: 26px;
  }

  form {
    background-color: #ffffff;
    padding: 30px 40px;
    border-radius: 10px;
    box-shadow: 0 4px 12px rgba(231, 76, 60, 0.15);
    max-width: 500px;
    width: 100%;
  }

  input[type="text"],
  input[type="password"],
  input[type="email"],
  input[type="file"] {
    width: 100%;
    padding: 12px 15px;
    margin: 10px 0;
    border: 1px solid #f5c6cb;
    border-radius: 6px;
    font-size: 14px;
    transition: border-color 0.3s ease;
  }

  input:focus {
    border-color: #e74c3c;
    outline: none;
  }

  label {
    margin-right: 10px;
    font-weight: 500;
    color: #c0392b;
  }

  input[type="radio"] {
    margin: 0 10px 10px 0;
    transform: scale(1.1);
    accent-color: #e74c3c;
  }

  button[type="submit"] {
    width: 100%;
    padding: 12px;
    background-color: #e74c3c;
    color: #ffffff;
    border: none;
    border-radius: 6px;
    font-size: 16px;
    font-weight: bold;
    cursor: pointer;
    margin-top: 15px;
    transition: background-color 0.3s ease;
  }

  button[type="submit"]:hover {
    background-color: #c0392b;
  }

  @media (max-width: 500px) {
    form {
      padding: 25px;
    }
  }
</style>

  <title>Blogging App</title>
</head>
