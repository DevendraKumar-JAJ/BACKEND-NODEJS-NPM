<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Bloggin</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" href="/blog">Add Blog</a>
        </li>
        <li class="nav-item dropdown">
          <a
            class="nav-link dropdown-toggle"
            href="#"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <%= user.firstName %>
          </a>
          <ul class="dropdown-menu">
            <% if (!logged) { %>
            <li>
              <a class="dropdown-item" href="/user/signup">Sign Up</a>
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="nav-link" href="/blog/all">My Blogs</a>
            </li>
            <% } %>
            <li class="nav-item">
              <a class="dropdown-item" href="/user/<%= type.toLowerCase() %>"
                ><%= value %></a
              >
            </li>
          </ul>
        </li>
      </ul>
      <img style="width: 50px" src="<%=user?.profileImage%>" alt="avatar" />
    </div>
  </div>
</nav>
